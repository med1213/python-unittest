# from django.test import TestCase
# from apps.live_data.models import LiveData

# class LiveDataTest(TestCase):
#     def setUp(self):
#         self.my_model = LiveData.objects.create(title="Test", status=True)

#     def test_model(self):
#         my_model = LiveData.objects.get(title="Test", status=True)
#         self.assertEqual(my_model.title, "Test")
#         self.assertEqual(my_model.status, True)
